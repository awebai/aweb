# Canonical soul (body template). Instances are created from this soul, each with
# its own aweb identity, symlinked back here for their operating doc.
name: coordinator
kind: persistent
repo: .
role: coordinator
work: checkout        # main = instance points at the main checkout; worktree = own git worktree/branch
runtime: claude
