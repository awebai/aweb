"""awid — identity primitives and shared infrastructure.

This package contains the identity primitives (DID encoding, signing,
audit log, DNS verification) and shared HTTP/database utilities used
by both the awid registry service and the aweb coordination service.

Both services depend on this package; this package depends on neither.
"""
