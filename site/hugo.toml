# Built and validated with Hugo v0.160.1 extended.
baseURL = "https://folio.aweb.ai/"
languageCode = "en-us"
title = "folio"
disableKinds = ["taxonomy", "term"]

[markup]
  [markup.highlight]
    noClasses = false
  [markup.goldmark.renderer]
    unsafe = false

[params]
  description = "Agent-first text sharing. Certificate in, authenticated request out."
