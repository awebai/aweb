# Built binary
/aw

# GoReleaser writes release metadata here before compiling. Keep its own output
# from making otherwise-clean release binaries report vcs.modified=true.
/dist/

# Test binaries
*.test

# Coverage
coverage.out

.cache/

# IDE
.idea/
.vscode/
.mcp.json

# OS files
.DS_Store
.beadhub
.aw/
.beads/
.env*
*.emacs-bu*

# npm platform binaries (placed by CI, not committed)
npm/*/bin/aw
npm/*/bin/aw.exe
# Keep the JS launcher (it's a node script, not a binary)
!npm/aw/bin/aw
